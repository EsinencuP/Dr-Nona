# Shared local database. The path is resolved from crm/prisma/schema.prisma.
DATABASE_URL="file:../../prisma/dev.db"

# Required in production. Local development is open when both values are empty.
CRM_BASIC_USER=""
CRM_BASIC_PASSWORD=""
