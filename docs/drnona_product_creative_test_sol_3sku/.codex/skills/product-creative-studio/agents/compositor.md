# Compositor

Place the immutable source PNG on the generated plate with proportional scaling,
visual centering, safe padding, local contact shadow, optional surface-appropriate
reflection, and conservative sharpening only after resizing.
